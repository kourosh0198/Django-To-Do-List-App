# 📝 Todo List

A simple single-page to-do list web app built with **Django** and **SQLite**.

## Features

- Add new tasks
- Edit existing tasks
- Remove tasks
- Mark tasks as done

## Tech Stack

- Python / Django
- SQLite
- HTML / CSS / JavaScript

## Installation

```bash
git clone https://github.com/kourosh0198/todo-list.git
cd todo-list
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Then open `http://127.0.0.1:8000/` in your browser.

---

# 📝 لیست کارها (تودو)

یک اپلیکیشن ساده‌ی تک‌صفحه‌ای برای مدیریت کارها، ساخته شده با **جنگو (Django)** و **SQLite**.

## قابلیت‌ها

- افزودن کار جدید
- ویرایش کارها
- حذف کارها
- علامت‌گذاری کارها به‌عنوان انجام‌شده

## تکنولوژی‌های استفاده‌شده

- پایتون / جنگو
- SQLite
- HTML / CSS / جاوااسکریپت

## نصب و راه‌اندازی

```bash
git clone https://github.com/kourosh0198/todo-list.git
cd todo-list
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

سپس آدرس `http://127.0.0.1:8000/` را در مرورگر باز کنید.

